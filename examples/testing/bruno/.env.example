# Values injected into the Bruno CLI with --env-var (see README).
# Prism accepts any non-empty value; a real deployment needs real credentials.
PARCELIO_API_KEY=demo-api-key
PARCELIO_OAUTH_TOKEN=demo-oauth-token
