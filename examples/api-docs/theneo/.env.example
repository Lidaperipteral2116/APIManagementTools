# Theneo API key. Create one in the Theneo dashboard (workspace settings, API keys).
# Used by `theneo login --token "$THENEO_API_KEY"` and by the GitHub Action as secrets.THENEO_SECRET.
THENEO_API_KEY=
