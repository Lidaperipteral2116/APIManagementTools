/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { packagesGetPackage } from "../../funcs/packagesGetPackage.js";
import { GetPackageRequest$zodSchema } from "../../models/getpackageop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetPackageRequest$zodSchema,
};

export const tool$packagesGetPackage: ToolDefinition<typeof args> = {
  name: "get_package",
  description:
    `Fetch one package by id (pkg_...): weight, dimensions, declared value, contents and the tracking number assigned at dispatch.`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await packagesGetPackage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
