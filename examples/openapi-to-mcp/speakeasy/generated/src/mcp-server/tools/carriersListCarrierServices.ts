/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { carriersListCarrierServices } from "../../funcs/carriersListCarrierServices.js";
import { ListCarrierServicesRequest$zodSchema } from "../../models/listcarrierservicesop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListCarrierServicesRequest$zodSchema,
};

export const tool$carriersListCarrierServices: ToolDefinition<typeof args> = {
  name: "carriers-list-carrier-services",
  description: `List carrier services

Returns the service levels a carrier offers (for example ground,
two-day, overnight) with their \`service_code\`, transit-time
estimate, and package limits. The \`service_code\` is what you pass
to \`createShipment\` and \`quoteRates\`.
`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await carriersListCarrierServices(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
