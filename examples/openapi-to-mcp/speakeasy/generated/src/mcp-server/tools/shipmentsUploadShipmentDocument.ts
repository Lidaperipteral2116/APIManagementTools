/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { shipmentsUploadShipmentDocument } from "../../funcs/shipmentsUploadShipmentDocument.js";
import { UploadShipmentDocumentRequest$zodSchema } from "../../models/uploadshipmentdocumentop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UploadShipmentDocumentRequest$zodSchema,
};

export const tool$shipmentsUploadShipmentDocument: ToolDefinition<typeof args> =
  {
    name: "shipments-upload-shipment-document",
    description: `Upload a shipment document

Attaches a document such as a commercial invoice or a dangerous-goods
declaration to a shipment. Accepted types are PDF, PNG, and JPEG up
to 10 MB. International shipments must have a \`commercial_invoice\`
document before \`dispatchShipment\` will succeed.
`,
    scopes: ["write"],
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await shipmentsUploadShipmentDocument(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
