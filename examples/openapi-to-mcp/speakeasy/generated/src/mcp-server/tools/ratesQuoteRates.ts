/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ratesQuoteRates } from "../../funcs/ratesQuoteRates.js";
import { RateRequest$zodSchema } from "../../models/raterequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: RateRequest$zodSchema,
};

export const tool$ratesQuoteRates: ToolDefinition<typeof args> = {
  name: "rates-quote-rates",
  description: `Quote rates

Returns a price and transit estimate for every enabled carrier
service that can move the given packages between the two addresses.
Quotes are valid for 15 minutes. Nothing is created; this call is
safe to make repeatedly while a user compares options.
`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await ratesQuoteRates(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
