/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { packagesAddPackage } from "../../funcs/packagesAddPackage.js";
import { AddPackageRequest$zodSchema } from "../../models/addpackageop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AddPackageRequest$zodSchema,
};

export const tool$packagesAddPackage: ToolDefinition<typeof args> = {
  name: "packages-add-package",
  description: `Add a package to a shipment

Adds a package to a \`draft\` shipment. Weight and dimensions are
required because carriers price on dimensional weight. Once a
shipment is dispatched its packages are frozen and this returns
\`409\`.
`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await packagesAddPackage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
