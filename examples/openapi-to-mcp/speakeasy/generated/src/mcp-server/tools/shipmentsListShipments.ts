/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { shipmentsListShipments } from "../../funcs/shipmentsListShipments.js";
import { ListShipmentsRequest$zodSchema } from "../../models/listshipmentsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListShipmentsRequest$zodSchema.optional(),
};

export const tool$shipmentsListShipments: ToolDefinition<typeof args> = {
  name: "shipments-list-shipments",
  description: `List shipments

Returns shipments belonging to the authenticated account, newest
first. Filter by \`status\` to find shipments in a given lifecycle
stage, or by \`carrier_id\` to see everything moving with one carrier.
Results are paginated with a cursor; follow \`next_cursor\` until it is
\`null\`.
`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await shipmentsListShipments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
