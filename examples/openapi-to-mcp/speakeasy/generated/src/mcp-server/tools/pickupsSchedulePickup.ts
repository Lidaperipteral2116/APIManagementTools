/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { pickupsSchedulePickup } from "../../funcs/pickupsSchedulePickup.js";
import { PickupCreate$zodSchema } from "../../models/pickupcreate.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PickupCreate$zodSchema,
};

export const tool$pickupsSchedulePickup: ToolDefinition<typeof args> = {
  name: "pickups-schedule-pickup",
  description: `Schedule a pickup

Asks a carrier to collect one or more dispatched shipments from an
address within a time window. All shipments in one pickup must use
the same carrier. Carriers typically need at least two hours' notice
and return \`422\` for windows they cannot honour.
`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await pickupsSchedulePickup(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
