/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { shipmentsGetShipmentLabel } from "../../funcs/shipmentsGetShipmentLabel.js";
import { GetShipmentLabelRequest$zodSchema } from "../../models/getshipmentlabelop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetShipmentLabelRequest$zodSchema,
};

export const tool$shipmentsGetShipmentLabel: ToolDefinition<typeof args> = {
  name: "shipments-get-shipment-label",
  description: `Download the shipping label

Returns the carrier label for a dispatched shipment. Choose the
format with the \`Accept\` header: \`application/pdf\` for a printable
page, or \`image/png\` for a 4x6 inch thermal-printer image. Draft
shipments have no label and return \`409\`.
`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await shipmentsGetShipmentLabel(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
