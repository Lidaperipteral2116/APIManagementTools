/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { shipmentsCancelShipment } from "../../funcs/shipmentsCancelShipment.js";
import { CancelShipmentRequest$zodSchema } from "../../models/cancelshipmentop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CancelShipmentRequest$zodSchema,
};

export const tool$shipmentsCancelShipment: ToolDefinition<typeof args> = {
  name: "shipments-cancel-shipment",
  description: `Cancel a shipment

Cancels a shipment. Draft shipments are deleted outright. Dispatched
shipments are voided with the carrier, which may take up to a few
minutes; the shipment moves to \`cancelled\` once the carrier confirms.
Shipments that are already \`in_transit\` or \`delivered\` cannot be
cancelled and return \`409\`.
`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await shipmentsCancelShipment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
