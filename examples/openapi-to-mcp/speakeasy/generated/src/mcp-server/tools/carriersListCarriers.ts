/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { carriersListCarriers } from "../../funcs/carriersListCarriers.js";
import { ListCarriersRequest$zodSchema } from "../../models/listcarriersop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListCarriersRequest$zodSchema.optional(),
};

export const tool$carriersListCarriers: ToolDefinition<typeof args> = {
  name: "carriers-list-carriers",
  description: `List carriers

Returns the carriers available to your account. Carriers you have
connected credentials for are marked \`enabled: true\`; the rest are
listed so you can see what is supported. Use \`listCarrierServices\`
to get the service codes needed by \`createShipment\`.
`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await carriersListCarriers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
