/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksGetWebhookSubscription } from "../../funcs/webhooksGetWebhookSubscription.js";
import { GetWebhookSubscriptionRequest$zodSchema } from "../../models/getwebhooksubscriptionop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetWebhookSubscriptionRequest$zodSchema,
};

export const tool$webhooksGetWebhookSubscription: ToolDefinition<typeof args> =
  {
    name: "get_webhook_subscription",
    description:
      `Fetch one webhook subscription: target URL, subscribed event types, active flag and 24-hour delivery health. The signing secret is never returned here.`,
    scopes: ["read"],
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await webhooksGetWebhookSubscription(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
