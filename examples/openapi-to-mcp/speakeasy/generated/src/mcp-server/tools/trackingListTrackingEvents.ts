/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { trackingListTrackingEvents } from "../../funcs/trackingListTrackingEvents.js";
import { ListTrackingEventsRequest$zodSchema } from "../../models/listtrackingeventsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListTrackingEventsRequest$zodSchema,
};

export const tool$trackingListTrackingEvents: ToolDefinition<typeof args> = {
  name: "tracking-list-tracking-events",
  description: `List tracking events

Returns every scan event the carrier has reported for the shipment,
oldest first, so the list reads as a timeline. Events are appended
as they arrive; subscribe to the \`shipment.status_changed\` webhook
instead of polling if you need them in real time.
`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await trackingListTrackingEvents(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
