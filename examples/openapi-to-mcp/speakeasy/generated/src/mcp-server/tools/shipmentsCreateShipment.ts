/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { shipmentsCreateShipment } from "../../funcs/shipmentsCreateShipment.js";
import { CreateShipmentRequest$zodSchema } from "../../models/createshipmentop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateShipmentRequest$zodSchema,
};

export const tool$shipmentsCreateShipment: ToolDefinition<typeof args> = {
  name: "shipments-create-shipment",
  description: `Create a shipment

Creates a shipment in \`draft\` status from a sender address, a
recipient address, one or more packages, and a carrier service.
No label is purchased and nothing is sent to the carrier until you
call \`dispatchShipment\`. Use \`quoteRates\` first if you need a price
before committing to a service.

Send an \`Idempotency-Key\` header to make retries safe.
`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await shipmentsCreateShipment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
