/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksCreateWebhookSubscription } from "../../funcs/webhooksCreateWebhookSubscription.js";
import { WebhookSubscriptionCreate$zodSchema } from "../../models/webhooksubscriptioncreate.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: WebhookSubscriptionCreate$zodSchema,
};

export const tool$webhooksCreateWebhookSubscription: ToolDefinition<
  typeof args
> = {
  name: "webhooks-create-webhook-subscription",
  description: `Create a webhook subscription

Registers an HTTPS endpoint to receive shipment events. The
response includes a \`secret\` exactly once; use it to verify the
\`Parcelio-Signature\` header on incoming deliveries. Deliveries are
retried with exponential backoff for up to 24 hours on non-2xx
responses.
`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await webhooksCreateWebhookSubscription(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
