/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { shipmentsDispatchShipment } from "../../funcs/shipmentsDispatchShipment.js";
import { DispatchShipmentRequest$zodSchema } from "../../models/dispatchshipmentop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DispatchShipmentRequest$zodSchema,
};

export const tool$shipmentsDispatchShipment: ToolDefinition<typeof args> = {
  name: "dispatch_shipment",
  description:
    `Purchase the carrier label and hand a draft shipment to the carrier. Irreversible and charges the account balance; confirm with the user before calling. Returns the shipment with tracking_number set. Fails with 409 if the shipment is not in draft, 402 if the balance is insufficient.`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await shipmentsDispatchShipment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
