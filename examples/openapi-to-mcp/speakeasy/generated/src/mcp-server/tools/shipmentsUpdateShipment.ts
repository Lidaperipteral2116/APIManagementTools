/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { shipmentsUpdateShipment } from "../../funcs/shipmentsUpdateShipment.js";
import { UpdateShipmentRequest$zodSchema } from "../../models/updateshipmentop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateShipmentRequest$zodSchema,
};

export const tool$shipmentsUpdateShipment: ToolDefinition<typeof args> = {
  name: "update_shipment",
  description:
    `Change reference, service_code, ship_to, label_format or metadata on a shipment that is still in draft. Only the fields sent are changed. Returns 409 once the shipment has been dispatched.`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await shipmentsUpdateShipment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
