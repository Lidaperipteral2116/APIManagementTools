/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { trackingGetTrackingByNumber } from "../../funcs/trackingGetTrackingByNumber.js";
import { GetTrackingByNumberRequest$zodSchema } from "../../models/gettrackingbynumberop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetTrackingByNumberRequest$zodSchema,
};

export const tool$trackingGetTrackingByNumber: ToolDefinition<typeof args> = {
  name: "tracking-get-tracking-by-number",
  description: `Track by tracking number

Looks up current status and the most recent scan events for a
carrier tracking number. Works for shipments created through
Parcelio and, for supported carriers, for any tracking number the
carrier recognises. This endpoint is rate-limited more strictly than
the rest of the API (see \`429\`).
`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await trackingGetTrackingByNumber(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
