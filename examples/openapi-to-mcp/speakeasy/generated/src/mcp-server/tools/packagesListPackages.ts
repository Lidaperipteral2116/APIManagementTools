/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { packagesListPackages } from "../../funcs/packagesListPackages.js";
import { ListPackagesRequest$zodSchema } from "../../models/listpackagesop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListPackagesRequest$zodSchema,
};

export const tool$packagesListPackages: ToolDefinition<typeof args> = {
  name: "list_packages",
  description:
    `List the packages that belong to one shipment, with weight, dimensions and (after dispatch) each package's tracking number. Cursor-paginated: pass next_cursor back as cursor until it is null.`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await packagesListPackages(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
