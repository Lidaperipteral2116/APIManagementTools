/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { addressesValidateAddress } from "../../funcs/addressesValidateAddress.js";
import { Address$zodSchema } from "../../models/address.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: Address$zodSchema,
};

export const tool$addressesValidateAddress: ToolDefinition<typeof args> = {
  name: "addresses-validate-address",
  description: `Validate an address

Checks an address against carrier and postal databases and returns
a normalised version with any corrections applied. Use the
\`residential\` flag in the response to pick the right service level;
several carriers surcharge residential deliveries.
`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await addressesValidateAddress(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
