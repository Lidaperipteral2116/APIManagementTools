/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksDeleteWebhookSubscription } from "../../funcs/webhooksDeleteWebhookSubscription.js";
import { DeleteWebhookSubscriptionRequest$zodSchema } from "../../models/deletewebhooksubscriptionop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteWebhookSubscriptionRequest$zodSchema,
};

export const tool$webhooksDeleteWebhookSubscription: ToolDefinition<
  typeof args
> = {
  name: "webhooks-delete-webhook-subscription",
  description: `Delete a webhook subscription

Stops deliveries to the endpoint immediately. In-flight retries are
abandoned. This cannot be undone; create a new subscription to
resume.
`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await webhooksDeleteWebhookSubscription(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
