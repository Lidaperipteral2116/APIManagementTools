/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { packagesRemovePackage } from "../../funcs/packagesRemovePackage.js";
import { RemovePackageRequest$zodSchema } from "../../models/removepackageop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: RemovePackageRequest$zodSchema,
};

export const tool$packagesRemovePackage: ToolDefinition<typeof args> = {
  name: "remove_package",
  description:
    `Remove a package from a draft shipment. Not allowed once the shipment is dispatched (409). Deleting the last package leaves an empty shipment that cannot be dispatched.`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await packagesRemovePackage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
