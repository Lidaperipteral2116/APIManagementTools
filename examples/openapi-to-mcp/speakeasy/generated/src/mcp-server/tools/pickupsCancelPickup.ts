/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { pickupsCancelPickup } from "../../funcs/pickupsCancelPickup.js";
import { CancelPickupRequest$zodSchema } from "../../models/cancelpickupop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CancelPickupRequest$zodSchema,
};

export const tool$pickupsCancelPickup: ToolDefinition<typeof args> = {
  name: "cancel_pickup",
  description:
    `Cancel a scheduled carrier pickup. Returns 409 if the driver has already completed it. Does not cancel the shipments in the pickup.`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await pickupsCancelPickup(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
