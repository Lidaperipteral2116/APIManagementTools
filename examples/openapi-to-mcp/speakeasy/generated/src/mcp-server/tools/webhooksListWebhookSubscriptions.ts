/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksListWebhookSubscriptions } from "../../funcs/webhooksListWebhookSubscriptions.js";
import { ListWebhookSubscriptionsRequest$zodSchema } from "../../models/listwebhooksubscriptionsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListWebhookSubscriptionsRequest$zodSchema.optional(),
};

export const tool$webhooksListWebhookSubscriptions: ToolDefinition<
  typeof args
> = {
  name: "webhooks-list-webhook-subscriptions",
  description: `List webhook subscriptions

Returns the webhook subscriptions configured for your account,
including each subscription's delivery health over the last 24
hours.
`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await webhooksListWebhookSubscriptions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
