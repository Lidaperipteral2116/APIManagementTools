/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { carriersGetCarrier } from "../../funcs/carriersGetCarrier.js";
import { GetCarrierRequest$zodSchema } from "../../models/getcarrierop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetCarrierRequest$zodSchema,
};

export const tool$carriersGetCarrier: ToolDefinition<typeof args> = {
  name: "get_carrier",
  description:
    `Fetch one carrier by id (car_ups, car_fedex, ...): whether the account has credentials for it, the countries it ships from and its public tracking URL template.`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await carriersGetCarrier(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
