/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksTestWebhookSubscription } from "../../funcs/webhooksTestWebhookSubscription.js";
import { TestWebhookSubscriptionRequest$zodSchema } from "../../models/testwebhooksubscriptionop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TestWebhookSubscriptionRequest$zodSchema,
};

export const tool$webhooksTestWebhookSubscription: ToolDefinition<typeof args> =
  {
    name: "test_webhook_subscription",
    description:
      `Send a synthetic shipment.status_changed event to a webhook subscription's URL and report whether the endpoint answered 2xx, the status code and the round-trip time. Safe to repeat.`,
    scopes: ["write"],
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await webhooksTestWebhookSubscription(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
