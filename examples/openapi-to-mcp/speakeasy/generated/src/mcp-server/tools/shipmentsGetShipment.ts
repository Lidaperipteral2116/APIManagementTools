/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { shipmentsGetShipment } from "../../funcs/shipmentsGetShipment.js";
import { GetShipmentRequest$zodSchema } from "../../models/getshipmentop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetShipmentRequest$zodSchema,
};

export const tool$shipmentsGetShipment: ToolDefinition<typeof args> = {
  name: "shipments-get-shipment",
  description: `Get a shipment

Returns the full shipment record including its packages, the
currently assigned carrier service, cost, and the latest tracking
status. Use \`listTrackingEvents\` for the complete scan history.
`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await shipmentsGetShipment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
