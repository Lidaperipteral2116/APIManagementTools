/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { pickupsGetPickup } from "../../funcs/pickupsGetPickup.js";
import { GetPickupRequest$zodSchema } from "../../models/getpickupop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetPickupRequest$zodSchema,
};

export const tool$pickupsGetPickup: ToolDefinition<typeof args> = {
  name: "pickups-get-pickup",
  description: `Get a pickup

Returns a scheduled pickup, including the carrier confirmation
number once the carrier has acknowledged it.
`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await pickupsGetPickup(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
