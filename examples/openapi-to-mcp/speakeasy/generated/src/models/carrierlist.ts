/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { Carrier, Carrier$zodSchema } from "./carrier.js";

/**
 * Cursor pagination envelope fields shared by every list response.
 */
export type CarrierList = {
  data: Array<Carrier>;
  next_cursor: string | null;
  has_more: boolean;
};

export const CarrierList$zodSchema: z.ZodType<CarrierList> = z.object({
  data: z.array(Carrier$zodSchema),
  has_more: z.boolean().describe("True when another page exists."),
  next_cursor: z.string().nullable().describe(
    "Pass as `cursor` to fetch the next page. `null` when this is the last page.",
  ),
}).describe("Cursor pagination envelope fields shared by every list response.");
