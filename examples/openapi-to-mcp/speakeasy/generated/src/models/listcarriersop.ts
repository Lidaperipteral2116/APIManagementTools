/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { CarrierList, CarrierList$zodSchema } from "./carrierlist.js";
import { Problem, Problem$zodSchema } from "./problem.js";

export type ListCarriersRequest = {
  limit?: number | undefined;
  cursor?: string | undefined;
  enabled?: boolean | undefined;
};

export const ListCarriersRequest$zodSchema: z.ZodType<ListCarriersRequest> = z
  .object({
    cursor: z.string().describe(
      "Opaque cursor from a previous response's `next_cursor`. Omit for the first page.",
    ).optional(),
    enabled: z.boolean().describe(
      "Only return carriers with connected credentials.",
    ).optional(),
    limit: z.int().default(20).describe(
      "Maximum number of items to return per page.",
    ),
  });

export type ListCarriersResponse = CarrierList | Problem;

export const ListCarriersResponse$zodSchema: z.ZodType<ListCarriersResponse> = z
  .union([
    CarrierList$zodSchema,
    Problem$zodSchema,
  ]);
