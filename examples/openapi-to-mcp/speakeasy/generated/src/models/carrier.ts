/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";

export type Carrier = {
  id: string;
  name: string;
  enabled: boolean;
  countries: Array<string>;
  tracking_url_template?: string | null | undefined;
};

export const Carrier$zodSchema: z.ZodType<Carrier> = z.object({
  countries: z.array(z.string()).describe(
    "ISO 3166-1 alpha-2 codes the carrier can ship from.",
  ),
  enabled: z.boolean().describe(
    "Whether this account has connected credentials for the carrier.",
  ),
  id: z.string(),
  name: z.string(),
  tracking_url_template: z.string().nullable().optional().describe(
    "URL with `{tracking_number}` placeholder for the carrier's own tracking page.",
  ),
});
