/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { Address, Address$zodSchema } from "./address.js";

export type Correction = { field: string; original: string; corrected: string };

export const Correction$zodSchema: z.ZodType<Correction> = z.object({
  corrected: z.string(),
  field: z.string(),
  original: z.string(),
});

export type AddressValidation = {
  valid: boolean;
  normalized: Address;
  corrections?: Array<Correction> | undefined;
  residential?: boolean | null | undefined;
};

export const AddressValidation$zodSchema: z.ZodType<AddressValidation> = z
  .object({
    corrections: z.array(z.lazy(() => Correction$zodSchema)).optional()
      .describe("Fields that were changed during normalisation."),
    normalized: Address$zodSchema,
    residential: z.boolean().nullable().optional().describe(
      "Residential classification, or `null` if the carrier could not determine it.",
    ),
    valid: z.boolean().describe("Whether the address is deliverable."),
  });
