/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";
import { Address, Address$zodSchema } from "./address.js";

export const Status = {
  Requested: "requested",
  Confirmed: "confirmed",
  Completed: "completed",
  Cancelled: "cancelled",
} as const;
export type Status = ClosedEnum<typeof Status>;

export const Status$zodSchema = z.enum([
  "requested",
  "confirmed",
  "completed",
  "cancelled",
]);

export type Pickup = {
  carrier_id: string;
  address: Address;
  window_start: string;
  window_end: string;
  shipment_ids: Array<string>;
  instructions?: string | undefined;
  id: string;
  status: Status;
  confirmation_number?: string | null | undefined;
  created_at: string;
};

export const Pickup$zodSchema: z.ZodType<Pickup> = z.object({
  address: Address$zodSchema,
  carrier_id: z.string(),
  confirmation_number: z.string().nullable().optional().describe(
    "Carrier's confirmation reference, set once `confirmed`.",
  ),
  created_at: z.iso.datetime({ offset: true }),
  id: z.string(),
  instructions: z.string().optional().describe(
    "Shown to the driver, for example \"Loading dock B, ring bell\".",
  ),
  shipment_ids: z.array(z.string()),
  status: Status$zodSchema,
  window_end: z.iso.datetime({ offset: true }),
  window_start: z.iso.datetime({ offset: true }),
});
