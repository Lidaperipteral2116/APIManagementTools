# Copy to .env and load it (set -a; source .env; set +a). The server reads only process.env.
# Any non-empty value works against the Prism mock.
PARCELIO_BASE_URL=http://127.0.0.1:4010
PARCELIO_API_KEY=demo
PARCELIO_OAUTH_TOKEN=demo
